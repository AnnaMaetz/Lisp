(define (last-pair l)
  (define (length l)
    (if (null? l)
        0
        (+ 1 (length (cdr l)))))
  (define (list-ref l n)
    (if (= n 0)
        (car l)
        (list-ref (cdr l) (- n 1))
    )
  )
  (list-ref l (- (length l) 1))
)
(last-pair (list 23 11 18 93))
